<UserControl x:Class="SnakeGame.MVVM.View.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SnakeGame.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border VerticalAlignment="Center"
            HorizontalAlignment="Center">
        <StackPanel>
            <RadioButton Content="Start Game"
                         FontSize="30"
                         FontWeight="Normal"
                         Foreground="LightGray"
                         Command="{Binding DataContext.StartGameCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>
    </Border>
</UserControl>
